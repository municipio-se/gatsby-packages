{
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "examples/*"
    ]
  },
  "scripts": {
    "lint": "eslint '**/*.{js,jsx}'",
    "format": "prettier --write '**/*.{js,jsx,json,md,mdx,yml,html,css}'",
    "test": "echo \"Write tests! -> https://gatsby.dev/unit-testing\" && exit 1"
  },
  "devDependencies": {
    "babel-eslint": "^10.1.0",
    "eslint": "^7.14.0",
    "eslint-plugin-import": "^2.22.1",
    "eslint-plugin-react": "^7.21.5",
    "prettier": "^2.2.0"
  }
}
